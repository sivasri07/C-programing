#include <stdio.h>
int main() 
{
int num, tens, units, swapped;
scanf("%d", &num);
if (num >= 10 && num <= 99 && num % 10 != 0 && num / 10 != 0) {
tens = num / 10;
units = num % 10;
swapped = units * 10 + tens;
printf("%d\n", swapped);
}
return 0;
}
